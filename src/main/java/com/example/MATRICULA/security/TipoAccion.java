package com.example.MATRICULA.security;

/**
 * Tipos de acción evaluables por el sistema de permisos.
 * Cada valor corresponde a una columna booleana de rol_funcionalidad.
 */
public enum TipoAccion {
    VER,
    INSERTAR,
    EDITAR,
    ELIMINAR,
    IMPRIMIR
}
